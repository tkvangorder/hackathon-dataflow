spring:
  application:
    name: source-charge
  cloud:
    config:
      #The label/branch that will be used to retrieve configuration from the configuration server.
      label: master
      name: source-charge,source-common
      fail-fast: true
