spring:
  application:
    name: source-order
  cloud:
    config:
      #The label/branch that will be used to retrieve configuration from the configuration server.
      label: master
      name: source-order,source-common
      fail-fast: true
