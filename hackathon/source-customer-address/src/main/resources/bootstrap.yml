spring:
  application:
    name: source-customer-address
  cloud:
    config:
      #The label/branch that will be used to retrieve configuration from the configuration server.
      label: master
      name: source-customer-address,source-common
      fail-fast: true
