spring:
  application:
    name: source-order-item
  cloud:
    config:
      #The label/branch that will be used to retrieve configuration from the configuration server.
      label: master
      name: source-order-item,source-common
      fail-fast: true
